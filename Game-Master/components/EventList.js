import { SafeAreaView, TextInput, Button, Alert, Text } from "react-native";

const EventList = () => {
  return (
    <SafeAreaView style={{ alignItems: "center", justifyContent: "center" }}>
      <Text>Events</Text>
    </SafeAreaView>
  );
};

export default EventList;
